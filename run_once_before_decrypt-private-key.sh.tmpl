#!/bin/sh

if [ ! -f "/home/isaaki/.config/chezmoi/key.txt" ]; then
    mkdir -p "/home/isaaki/.config/chezmoi"
    chezmoi age decrypt --output "/home/isaaki/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/home/isaaki/.config/chezmoi/key.txt"
fi
