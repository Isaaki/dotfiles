################
### MONITORS ###
################

$LG=LG Electronics LG ULTRAGEAR 109MAMB6UH71
$Samsung=Samsung Electric Company SyncMaster 0x4C523234
$Benq=BNQ BenQ xl2420t G8D00198SL0

# $LG=HDMI-A-2
# $Samsung=DP-4
# $Benq=DP-5

# See https://wiki.hyprland.org/Configuring/Monitors/
# Get current monitor info with "hyprctl monitors"
monitor=desc:$LG, 2560x1440@179, 0x0, 1, vrr, 1
# monitor=desc:$LG, 2560x1440@179, 0x0, 1
monitor=desc:$Samsung, 1920x1080@60, 2560x180, 1
monitor=desc:$Benq, 1920x1080@120, -1920x180, 1

# For new monitors
monitor = , preferred, auto, 1

# render {
#   cm_fs_passthrough = 1
# }


###### TEMPORARY TO NOT GET "error 0: Windows scRGB profiles are not supported" ######
# More info here: https://github.com/ValveSoftware/gamescope/issues/1825
debug {
  full_cm_proto = true
}


# Setup default monitor
workspace=1,defaultName:1,monitor:desc:$LG,default:true,persistent:true
workspace=4,defaultName:4,monitor:desc:$LG,default:false,persistent:true
workspace=5,defaultName:5,monitor:desc:$LG,default:false,persistent:true

workspace=2,defaultName:2,monitor:desc:$Benq,default:true,persistent:true

workspace=3,defaultName:3,monitor:desc:$Samsung,default:true,persistent:true
# workspace=5,defaultName:5,monitor:$Samsung,default:true,persistent:true

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = uwsm app -- wezterm
$fileManager = uwsm app -- dolphin
# $menu = uwsm app -- $(wofi --show drun --define=drun-print_desktop_file=true)
$menu = uwsm app -- $(rofi -mode "run" -show drun)

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = hyprpm reload -n

exec-once = uwsm app -- hyprpanel
exec-once = uwsm app -- ~/.scripts/wallpaper-changer.sh

exec-once=[workspace 1] uwsm app -- firefox

exec-once=[workspace 3 silent] uwsm app -- plexamp.desktop
exec-once=[workspace 3 silent] uwsm app -- youtube-music

exec-once=[workspace 5 silent] uwsm app -- steam

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/
# Add new ENV in .config/uwsm/env

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 2
    gaps_out = 2

    border_size = 1

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffdc) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = true

    layout = dwindle
}

#SmartGaps( automatic hides gaps if only one window is on the screen )
workspace = w[tv1], gapsout:0, gapsin:0
workspace = f[1], gapsout:0, gapsin:0
windowrulev2 = bordersize 0, floating:0, onworkspace:w[tv1]
windowrulev2 = rounding 0, floating:0, onworkspace:w[tv1]
windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]
windowrulev2 = rounding 0, floating:0, onworkspace:f[1]

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}


# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(

    # vrr = 1
}

experimental {
    xx_color_management_v4 = true
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
  input {
      kb_layout = alt-nord,us
      kb_variant =
      kb_model =
      kb_options =
      kb_rules =

      numlock_by_default = true

      repeat_rate = 30 
      repeat_delay = 250

      follow_mouse = 1

      accel_profile = flat
      # force_no_accel = true

      sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

      touchpad {
          natural_scroll = false
      }
  }

  bind = SUPER,A,exec, hyprctl switchxkblayout current next

###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
  $mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
  bind = $mainMod, T, exec, $terminal
  bind = $mainMod, C, killactive,
  bind = $mainMod, M, exec, uwsm stop
  bind = $mainMod, E, exec, $fileManager
  bind = $mainMod, V, togglefloating,
  bind = $mainMod, R, exec, $menu
  bind = $mainMod, F, fullscreen
  bind = $mainMod, P, pseudo, # dwindle
  bind = $mainMod, X, togglesplit, # dwindle
  bind = $mainMod ALT, L, exec, loginctl lock-session
  bind = $mainMod, Escape, exec, uwsm app -- ~/.scripts/btop-toggle.sh
  bind = $mainMod, Z, exec, uwsm app -- ~/.scripts/mpv-open-link.sh
  bind = $mainMod, Delete, exec, loginctl lock-session

# Move focus with mainMod + arrow keys
  bind = $mainMod, left, movefocus, l
  bind = $mainMod, right, movefocus, r
  bind = $mainMod, up, movefocus, u
  bind = $mainMod, down, movefocus, d
# Move focus with mainMod + hjkl
  bind = $mainMod, h, movefocus, l
  bind = $mainMod, j, movefocus, d
  bind = $mainMod, k, movefocus, u
  bind = $mainMod, l, movefocus, r

  bind = $mainMod SHIFT, h, movewindow, l
  bind = $mainMod SHIFT, j, movewindow, d
  bind = $mainMod SHIFT, k, movewindow, u
  bind = $mainMod SHIFT, l, movewindow, r

# Switch workspaces with mainMod + [0-9]
  bind = $mainMod, 1, workspace, 1
  bind = $mainMod, 2, workspace, 2
  bind = $mainMod, 3, workspace, 3
  bind = $mainMod, 4, workspace, 4
  bind = $mainMod, 5, workspace, 5
  bind = $mainMod, 6, workspace, 6
  bind = $mainMod, 7, workspace, 7
  bind = $mainMod, 8, workspace, 8
  bind = $mainMod, 9, workspace, 9
  bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
  bind = $mainMod SHIFT, 1, movetoworkspace, 1
  bind = $mainMod SHIFT, 2, movetoworkspace, 2
  bind = $mainMod SHIFT, 3, movetoworkspace, 3
  bind = $mainMod SHIFT, 4, movetoworkspace, 4
  bind = $mainMod SHIFT, 5, movetoworkspace, 5
  bind = $mainMod SHIFT, 6, movetoworkspace, 6
  bind = $mainMod SHIFT, 7, movetoworkspace, 7
  bind = $mainMod SHIFT, 8, movetoworkspace, 8
  bind = $mainMod SHIFT, 9, movetoworkspace, 9
  bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
  bind = $mainMod, S, togglespecialworkspace, magic
  bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
  bind = $mainMod, mouse_down, workspace, e+1
  bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
  bindm = $mainMod, mouse:272, movewindow
  bindm = $mainMod, mouse:273, resizewindow

# Plexamp media shortcuts
  # bind = SUPER CONTROL ALT, up, pass, class:^(Plexamp)
  # bind = SUPER CONTROL ALT, down, pass, class:^(Plexamp)
  # bind = SUPER CONTROL ALT, left, pass, class:^(Plexamp)
  # bind = SUPER CONTROL ALT, right, pass, class:^(Plexamp)
  binde = SUPER CONTROL ALT, up,    exec, playerctl --player=Plexamp volume 0.02+
  binde = SUPER CONTROL ALT, down,  exec, playerctl --player=Plexamp volume 0.02-
  bind = SUPER CONTROL ALT, left,   exec, playerctl --player=Plexamp play-pause
  bind = SUPER CONTROL ALT, right,  exec, playerctl --player=Plexamp next

# YTMusic media shortcuts
  # bind = SUPER CONTROL ALT, Prior, pass, class:^(com.github.th_ch.youtube_music)
  # bind = SUPER CONTROL ALT, Next, pass, class:^(com.github.th_ch.youtube_music)
  # bind = SUPER CONTROL ALT, J, pass, class:^(com.github.th_ch.youtube_music)
  # bind = SUPER CONTROL ALT, L, pass, class:^(com.github.th_ch.youtube_music)
  binde = SUPER CONTROL ALT, Prior, exec, playerctl --player=YoutubeMusic volume 0.02+
  binde = SUPER CONTROL ALT, Next,  exec, playerctl --player=YoutubeMusic volume 0.02-
  bind = SUPER CONTROL ALT, J,      exec, playerctl --player=YoutubeMusic play-pause
  bind = SUPER CONTROL ALT, L,      exec, playerctl --player=YoutubeMusic next

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Ignore maximize requests from apps. You'll probably like this.
  windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
  windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
  windowrule = workspace 5 silent, class:steam

# Btop
  windowrule = float, class:btop
  windowrule = persistentsize, class:btop
  windowrule = center, class:btop
  windowrule = size 1400 900, class:btop

# bluetui
  windowrule = float, class:bluetui
  windowrule = persistentsize, class:bluetui
  windowrule = center, class:bluetui
  windowrule = size 1400 900, class:bluetui

# Pavucontrol
  windowrule = float, class:org.pulseaudio.pavucontrol
  windowrule = persistentsize, class:org.pulseaudio.pavucontrol
  windowrule = center, class:org.pulseaudio.pavucontrol
  windowrule = size 1200 700, class:org.pulseaudio.pavucontrol

# Pulsemixer
  windowrule = float, class:pulsemixer
  windowrule = persistentsize, class:pulsemixer
  windowrule = center, class:pulsemixer
  windowrule = size 1200 700, class:pulsemixer

# Mpv
  windowrule = float, class:mpv
  windowrule = persistentsize, class:mpv
  windowrule = center, class:mpv
  windowrule = size 1200 700, class:mpv

# Games
  windowrule = immediate, class:^(cs2)$
  windowrule = workspace silent 5, class:^(steam_app_1174180)$
  windowrule = immediate, class:^(love)$

###############
### PLUGINS ###
###############

  plugin {
      csgo-vulkan-fix {
          res_w = 1920
          res_h = 1440

          # NOT a regex! This is a string and has to exactly match initial_class
          class = cs2

          # Whether to fix the mouse position. A select few apps might be wonky with this.
          fix_mouse = true
      }
  }
